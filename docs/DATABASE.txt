PROTOMUCK DATABASE REDESIGN
===========================

Design agreed 2026-08-27, revised same day to the module architecture.
This document is the authority for the new-database branch.  Where code
and this document disagree during development, this document wins until
amended.


1. IDENTITY

   Every object has two identities, both first class, both permanent:

   UUID (version 7)
       The primary key.  Time-ordered, minted at object creation, never
       reused, never changed.  Stable across servers, suitable for
       merging databases in the future.

   dbref
       The traditional #123 number.  Monotonic: a freshly created
       object takes the next number, and numbers are NEVER reused.
       dbref recycling is abolished; the aliasing bug where a stale
       #500 silently retargets to a new object cannot return.  Large
       gaps after mass deletion are accepted and are a feature: they
       push developers and players toward UUIDs.

   Short-form UUIDs are accepted on input wherever a unique prefix
   resolves (like git), and listings display the short form.  Full
   UUIDs are always stored on disk.

   The garbage object type is abolished.  Deleting an object removes it
   from both indexes and records a tombstone row: uuid, dbref, deleted
   timestamp, deleting player uuid.  Tombstone retention is set by a
   @tune parameter: from zero to forever, default on the order of 90
   days.  Programs holding a deleted object receive an OBJECT_DELETED
   mufevent.


2. OBJECT MODEL: CORE PLUS MODULES

   There is one concrete object class, DbObject.  What an object IS is
   determined by the modules attached to it, not by a class hierarchy.

   DbObject core (present on every object, versioned, not modular):
       uuid, dbref, name, location, flags, timestamps.

   TYPE modules: PLAYER, THING, ROOM, EXIT, MUFPROG.
       Exactly one per object, attached at creation by
       Database.Create<T>(), immutable for the object's life.
       Exclusivity is enforced by construction, not by runtime checks.
       Typeof() reports the type module.  Containment state (contents
       and exits vectors) lives on the type modules that can contain:
       ROOM, THING, PLAYER.

   FEATURE modules: PROPERTIES is the first; VEHICLE is the canonical
       future example.  Zero or more per object.  Two scopes:

       global      enabled for the whole game and auto-attached to
                   every object (PROPERTIES).  Recorded in the
                   manifest.
       individual  attached to specific objects (a THING that is a
                   vehicle).  Recorded in the object's file.

   Module state is divided in two, and the divide is structural:

       persistent  what the module hands the database for storage,
                   expressed ONLY in the standard value model below.
       transient   ordinary members on the module object that are
                   never handed over and can never reach disk.  The
                   PLAYER module's session data, descriptors, and
                   output queues live here: on the object, found
                   through the same lookups, gone at restart.

   Creation:

       Player *who = database().Create<Player>(name, where);
       DbObject *obj = database().GetByRef(ref);      // boundary only
       if (Player *p = obj->As<Player>()) { ... }     // null if absent

   Objects cannot be constructed outside Create<T>, so nothing can
   exist without identity or indexing, and Create is the single
   gatekeeper where quota enforcement can land later.

   Modules are designed to be dynamically loadable eventually (the
   contract does not care whether a module is compiled in or loaded
   from a shared object).  Compiled-in modules come first; dlopen
   delivery is a later step.  Unloading a module makes its data
   dormant, never lost: see section 4.


3. THE VALUE MODEL: HOW MODULES PERSIST STATE

   The database defines one closed set of value types:

       string, int, float, bool, ref (a uuid), ordered list,
       and chunk-ref (a hash into a shared content-addressed pool).

   A module's persistent state is a set of entries: namespaced key ->
   typed value.  Namespaces are reserved per module:

       $core/...      DbObject core fields
       $type/...      the type module's fields
       /...           PROPERTIES (player-visible props keep their
                      traditional paths)
       ~vehicle/...   an individual feature module

   PROPERTIES declares one entry per prop, giving per-prop versioning.
   MUFPROG stores source as a list of chunk-refs into the pool, giving
   per-line dedup and versioning.  The chunk pool is a database-level
   service any module may use; pruning garbage-collects unreferenced
   chunks.

   The database versions, dedups, prunes, rolls back, and diffs
   entries WITHOUT understanding them.  Modules alone know what the
   keys mean, and rebuild their in-memory state from their entry slice
   on load or rollback.  No module may define its own file format;
   this rule is what makes central versioning and module-absent
   durability possible.


4. MODULE DURABILITY

   Entries belonging to a module that is not currently loaded are
   dormant: the database continues to load, carry, version-freeze, and
   re-save them untouched.  The object keeps its attachment record.
   When the module returns, its data is exactly where it was.  Modules
   stamp a schema version inside their namespace so a newer module
   build can migrate older entries on first touch.


5. IN-MEMORY MODEL

   Two hash maps own lookup: uuid -> DbObject and dbref -> DbObject.
   There is NO dense array and NO db_top.  A mass-grief incident
   creates no million-entry hole because there is nothing to hole.

   Code holds pointers (to DbObject or to a specific module), not
   dbrefs.  Resolution through the maps happens at boundaries only:
   MUF primitive arguments, player input, disk load.  Eventually MUF
   gains an OBJECT type alongside DBREF and the interpreter resolves
   stack values straight to pointers.

   Deleted objects are unlinked from both maps but their memory is
   never freed while the server runs, so no held pointer can dangle.
   Holders can check IsDeleted().  Memory is reclaimed at restart.

   Paging: the design must allow payload to be paged in from disk in
   the future, though today everything stays in RAM.  Two rules
   follow: all field access goes through accessor methods, and
   identity is always resident even when payload is not.

   Contents and exits are std::vector<DbObject *>.  The old intrusive
   location/contents/exits/next dbref chains are abolished.


6. ON-DISK FORMAT

   One JSON file per object, named by UUID, git-sharded two levels
   deep to keep directories small:

       data/objects/01/8f/018f3a2b-....json      current state
       data/objects/01/8f/018f3a2b-....hist      append-only history

   The object file records: the type module name, the list of
   individually attached feature modules, and the entries grouped by
   namespace.  Object references serialize as UUIDs, including the
   contents and exits vectors.  MUF source is stored inside the object
   file (as chunk-refs), not in muf/<ref>.m.

   A manifest file at the data root stores the format version, the
   global revision counter, the next dbref, the globally enabled
   feature modules, the tombstone table, and the @tune parameters
   (parmfile.cfg moves into the manifest; help files and logs
   explicitly do NOT move into the database).

   Startup reads only the .json files; boot time is independent of
   retained history.  .hist is read only for rollback or historical
   queries.  Cold objects have no .hist file at all.

   Writes are crash-safe: append history first, then atomically rename
   the new .json over the old.  A crash between the two leaves a
   harmless, detectable duplicate.

   Flat-file support is stripped from the server.  A converter class
   (absorbing the old db_read machinery) performs one-way import of
   the old NeonMuck V2 format, minting UUIDs during load.


7. VERSIONING (copy on write)

   One global 64-bit revision counter, incremented by one per
   snapshot, shared by whole-database snapshots and single-object
   snapshots.  Objects do not have private counters; they have marker
   lists.  A global snapshot records a marker in the manifest; a
   single-object snapshot records a marker only in that object's
   file.  Nothing ever walks the database to snapshot.

   Every entry is stamped with the revision at which its value took
   effect.  Writing an entry:

       - identical to current value: write nothing at all.  A program
         rewriting 10,000 unchanged props inflates nothing.
       - changed, and no retained snapshot has seen the old value:
         overwrite in place.
       - changed, and a retained snapshot references the old value:
         move the old value to history, stamp the new one.

   History therefore grows with snapshots x entries-actually-changed,
   never with write volume.

   Deleting an entry writes a tombstone entry at the current rev, so a
   historical read below that rev still sees it and a current read
   does not.

   Reading at revision R: newest entry per key with rev <= R.

   Pruning: a history entry is droppable only when no retained marker
   falls in its lifetime, AND the newest entry below the oldest
   retained marker is kept per key, so aging out revs cannot orphan a
   live value.  Pruning also garbage-collects unreferenced chunks.

   Retention is time-based on markers via @tune (like 30 days).  A
   marker can be locked; locked markers never age out.  Use case: lock
   a snapshot of one program before a risky edit, roll back if needed.

   Only entries are versioned.  Transient module state has no COW and
   no revs.


8. MIGRATION AND OPERATIONS

   Development happens on the new-database branch; GinkoMUCK runs that
   branch as the live test.  The importer converts data/neon.db once;
   after verification the flat files are retired.

   Steps:
     1. New format working end to end: converter class absorbs the
        flat-file reader, server loads old format, writes new format,
        loads and runs from new format thereafter.  The in-memory
        model temporarily remains the legacy struct; the FILE format
        is module-shaped from day one so step 2 does not change it.
     2. Rebuild the in-memory model to DbObject plus modules, in small
        sections: write new methods, hunt down old call sites,
        modernize them.  Loop until the legacy struct and union are
        gone.  Recycling and garbage removal land here.
     3. Versioning: revision stamps, markers, history files, the
        chunk pool, pruning, @tune retention.
     4. (with 3) Tombstone table and OBJECT_DELETED event.
     5. Integration passes discussed case by case: sessions onto the
        PLAYER module, MUF OBJECT type, tunables fully in manifest,
        dlopen module delivery, further feature modules (VEHICLE as
        the model case).


=====================================================================
DEVELOPER REFERENCE
=====================================================================

This section documents the classes as they exist on the new-database
branch.  It grows as code lands.  Everything lives in namespace MUCK;
headers are in src/inc/.


Uuid  (Uuid.h, Uuid.cpp)

    Sixteen-byte UUID value type.

    Uuid::generate()              mint a fresh version 7 uuid
    Uuid::parse(text)             canonical 36-char form; nil on error
    Uuid::tryParse(text, out)     bool form of the above
    u.toString()                  "018f3a2b-7c41-7e39-9f2a-..."
    u.shortString(n)              first n hex digits, default 8
    u.matchesPrefix(hex)          git-style prefix test
    u.isNil()                     all-zero check
    ==, !=, <, std::hash          usable as any map key

    Version 7 means the top 48 bits are a millisecond timestamp:
    uuids sort by creation time.  Two uuids minted the same instant
    share a long prefix, so DISPLAY short forms may need more digits;
    matchesPrefix handles any length.


Database identity  (Database.h)

    database().uuidOf(ref)        uuid for a dbref (nil if invalid)
    database().refOf(uuid)        dbref for a uuid (NOTHING if none)
    database().assignUuid(r, u)   bind identity (loader/importer only)
    database().resolveUuidPrefix(s)
                                  unique-prefix lookup; NOTHING or
                                  AMBIGUOUS on failure

    newObject() mints a fresh uuid ALWAYS, including on a recycled
    garbage slot: a reused dbref is a new object and never inherits
    the old identity.


ObjectStore  (ObjectStore.h, ObjectStore.cpp)

    The only database write path.  Layout:

        <root>/manifest.json
        <root>/objects/aa/bb/<uuid>.json

    ObjectStore::isStore(path)    manifest present?
    store().setRoot(path)         bind the data root
    store().saveAll(dirtyOnly)    write manifest + objects; dirtyOnly
                                  skips objects without OBJECT_CHANGED
                                  (forced full into a fresh root)
    store().loadAll()             read an entire store
    store().saveObject(ref)       rewrite one object
    store().removeObject(ref)     delete one object file

    Object file schema (format 1):

        uuid, dbref, type         identity; type is the type module
                                  name: room, thing, exit, player,
                                  muf_program (garbage only until the
                                  legacy allocator dies in step 2)
        modules                   individually attached feature
                                  modules (empty for now)
        core                      name, location, owner, flags[4],
                                  powers[2], ts{...}
        type_data                 per type: dropto/home/value/pennies/
                                  password, contents[], exits[],
                                  dests[], source[]
        props                     flat array of {n, f, v}: full slash
                                  path, raw flag word, value typed by
                                  the flag's type bits

    Encodings and rules:

        refs        uuid string, or negative integer for the NOTHING,
                    AMBIGUOUS, HOME, NIL sentinels
        strings     latin-1 encoded losslessly to UTF-8 and back
        props       values the flat format never persisted (zero int,
                    empty string, NOTHING ref, TRUE_BOOLEXP lock) are
                    skipped; the compression flag is stripped and
                    values always stored uncompressed; prop refs stay
                    dbref integers until the step 2 memory rebuild
        chains      contents and exits serialize as ordered uuid
                    lists validated against legacy chain corruption
                    (location must equal container; one chain
                    membership per object per save)
        writes      content to <file>.tmp, then atomic rename

    Loading is two-phase: phase one creates every object and assigns
    identity, phase two wires refs, chains, and props (lock props
    parse by dbref and need every target to exist).  Phase one must
    leave links NOTHING; phase two only wires.  Do not reorder this.


FlatFileConverter  (FlatFileConverter.h, FlatFileConverter.cpp)

    flatConverter().import(f)     read a legacy flat dump, minting
                                  uuids; returns top dbref or -1

    One-way.  Nothing in the tree can write the flat format.  The
    server auto-detects: a store root loads through ObjectStore, a
    flat file imports and the first dump writes the store.  Offline:

        protomuck -convert data/old.db data/store


ProgramStore additions  (ProgramStore.h)

    programs().sourceLines(ref)   authoritative source, or null
    programs().setSourceLines(ref, lines)
    programs().dropSource(ref)

    The in-memory cache is authoritative; the object store serializes
    it inside the program's file.  muf/<ref>.m is read as a fallback
    for flat-era installs and refreshed on write for compatibility,
    and goes away entirely in a later step.


DbObject and modules  (DbObject.h, Modules.h, DbObject.cpp)  [step 2]

    The modernized object model, currently implemented as VIEWS over
    the legacy struct object so both worlds stay coherent while call
    sites migrate.  Code written against this API will not change when
    storage flips inside.

    DbObject core:
        obj->uuid(), obj->ref()             identity
        obj->name(), obj->setName(s)
        obj->location()                     DbObject pointers, not refs
        obj->owner(), obj->setOwner(o)
        obj->isDeleted()
        obj->typeName()                     "room", "player", ...

    Module access:
        obj->As<Player>()                   null if not that type
        obj->Get<Properties>()              any attached module
        obj->attach(make_unique<T>())       feature modules

    Lookup and creation:
        database().get(ref)                 shell for a dbref (lazy,
                                            stable pointer, null if
                                            invalid)
        database().get(uuid)
        database().Create<Thing>(name, owner)
                                            typed creation; minimal for
                                            now, the legacy create
                                            paths still do per-type
                                            setup and migrate here in
                                            later sections

    Type modules: Room (dropTo), Thing (home, value), Player (home,
    pennies, checkPassword, setPassword), Exit (destinations),
    MufProgram (source).  Room, Thing, Player derive from Container
    (contents(), exits(), returned as vectors of DbObject pointers).
    Calling exits() on an Exit is a compile error by construction.

    Feature modules: Properties (getString, getInt, getFloat, getRef,
    setString, setInt, remove, exists), auto-attached to every object.

    Migration pattern for step 2 sections: pick a legacy cluster (a
    struct field family or free-function group), give the module the
    method, repoint the call sites, delete the legacy declarations.
    Loop until the sp union and the raw chains have no readers.
